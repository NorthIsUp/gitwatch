"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HumiditySensorPlatformAccessory = void 0;
const baseSensorAccessory_1 = require("./baseSensorAccessory");
/**
 * Platform Accessory
 * An instance of this class is created for each accessory your platform registers
 * Each accessory may expose multiple services of different service types.
 */
class HumiditySensorPlatformAccessory extends baseSensorAccessory_1.BaseSensorPlatformAccessory {
    constructor(platform, accessory) {
        super(platform, accessory);
        this.platform = platform;
        this.accessory = accessory;
        this.currentState = 0.0;
    }
    createService() {
        return this.accessory.getService(this.platform.Service.HumiditySensor) ||
            this.accessory.addService(this.platform.Service.HumiditySensor);
    }
    configureSensor() {
        // set the service name, this is what is displayed as the default name on the Home app
        // in this example we are using the name we stored in the `accessory.context` in the `discoverDevices` method.
        this.service.getCharacteristic(this.platform.Characteristic.CurrentRelativeHumidity)
            .onGet(this.handleHomekitCurrentStateGet.bind(this));
    }
    updateCharacteristic(value) {
        this.service.updateCharacteristic(this.platform.Characteristic.CurrentRelativeHumidity, value);
    }
    initialValue() {
        return 0.0;
    }
}
exports.HumiditySensorPlatformAccessory = HumiditySensorPlatformAccessory;
//# sourceMappingURL=humiditySensorAccessory.js.map